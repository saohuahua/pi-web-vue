# Brand Asset Contract

新 Logo 和标识尚未提供 因此应用不新增虚构图形资产。

交付新资产时放入此目录 并同时提供以下信息：

- 横向和紧凑版 SVG
- 浅色和深色版本
- 最小展示尺寸和安全留白
- 主色及不允许搭配的状态色
- 产品显示名称和辅助文字规则

替换时优先修改应用中的品牌组件和 `DESIGN.md` token 不修改参考原型文件。

当前应用的临时标识由 `app/components/BrandMark.vue` 统一提供。替换新资产时只更新该组件及其 CSS 变体。
